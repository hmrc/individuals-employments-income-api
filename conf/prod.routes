->         /                     health.Routes
->         /                     definition.Routes


# NOTE: versioned API routes (e.g. v2.routes) are handled automatically
# via VersionRoutingRequestHandler and so there is no need to include them here
