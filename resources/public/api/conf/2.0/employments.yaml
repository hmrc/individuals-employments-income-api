get:
  $ref: "./employments_list.yaml"
  security:
    - User-Restricted:
      - read:self-assessment

post:
  $ref: "./employments_add.yaml"

  security:
    - User-Restricted:
      - write:self-assessment